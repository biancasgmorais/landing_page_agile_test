import styled from "styled-components";

export const FooterContainer = styled.div`
  background: linear-gradient(to left, rgba(0, 0, 0, 1), rgba(0, 0, 0, 0)), #0F1316;
  color: #fff;
  text-align: center;
  position: absolute;
  bottom: 0;
  width: 100%;
  height: 25%;
`;
